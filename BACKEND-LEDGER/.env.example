MONGO_URI=mongodb://127.0.0.1:27017/banking-ledger
JWT_SECRET=replace_with_a_strong_secret
FRONTEND_URL=http://localhost:5173

# Set to true only when email credentials are configured
ENABLE_EMAIL_NOTIFICATIONS=false

# App password mode (recommended)
EMAIL_USER=
EMAIL_APP_PASSWORD=

# OAuth2 mode (optional alternative)
CLIENT_ID=
CLIENT_SECRET=
REFRESH_TOKEN=
ACCESS_TOKEN=
